row:
   rowIndex:
      to_subject: variant
transformers:
    - cat_format_if:
        columns:
            - alteration
            - hugoSymbol
        to_object: alteration
        via_relation: variant_carries_alteration
        format_string: "{hugoSymbol}:{alteration}"
